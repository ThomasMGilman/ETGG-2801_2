layout(location=0) in vec3 position;
layout(location=1) in vec3 texCoord;
out vec2 v_texCoord;
void main(){
    vec3 transform = position * worldMatrix * scaling;
    gl_Position = vec4( transform.xy , -1, 1 );
    gl_PointSize = 1;
    v_texCoord = texCoord;
}
